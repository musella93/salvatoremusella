

## Plan: Add `/cv` route

Add a single line in `src/App.tsx` to map `/cv` to the existing `Index` component.

### Steps

1. In `src/App.tsx`, add `<Route path="/cv" element={<Index />} />` right after the existing `/` route.

No other files need to change. No new components, no navigation links.

